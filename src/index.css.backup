@tailwind base;
@tailwind components;
@tailwind utilities;

html[data-theme="light"] {
  color-scheme: light;
}
html[data-theme="dark"] {
  color-scheme: dark;
}

@layer base {
  html,
  body,
  #root {
    height: 100%;
  }
  body {
    @apply bg-canvas text-slate-100 font-sans antialiased;
    background-image: theme("backgroundImage.grid-glow");
    background-attachment: fixed;
  }
  html[data-theme="light"] body {
    @apply bg-slate-50 text-slate-900;
    background-image: none;
  }
  ::selection {
    @apply bg-brand-400/30;
  }
}

@layer components {
  .card {
    @apply rounded-xl2 border border-surface-border/70 bg-surface/70 backdrop-blur-sm shadow-card p-5;
  }
  html[data-theme="light"] .card {
    @apply bg-white border-slate-200 shadow-sm;
  }
  .btn {
    @apply inline-flex items-center justify-center gap-2 rounded-full px-4 py-2 text-sm font-semibold
      border border-surface-border/80 bg-surface-raised/80 text-slate-100 transition
      hover:border-brand-400/50 hover:bg-surface-raised active:scale-[0.98] disabled:opacity-40 disabled:pointer-events-none;
  }
  html[data-theme="light"] .btn {
    @apply border-slate-200 bg-white text-slate-800 hover:border-brand-400/60;
  }
  .btn-primary {
    @apply bg-gradient-to-r from-brand-500 to-brand-400 text-black border-transparent shadow-glow hover:brightness-110;
  }
  .btn-danger {
    @apply border-red-500/30 text-red-300 hover:bg-red-500/10;
  }
  .pill {
    @apply inline-flex items-center rounded-full border border-surface-border/70 bg-surface-raised/60 px-2.5 py-0.5 text-xs font-medium text-slate-300;
  }
  html[data-theme="light"] .pill {
    @apply border-slate-200 bg-slate-100 text-slate-600;
  }
  .input,
  .select,
  textarea.input-area {
    @apply w-full rounded-lg border border-surface-border/80 bg-surface-raised/60 px-3 py-2.5 text-sm text-slate-100
      outline-none transition focus:border-brand-400/70 focus:ring-2 focus:ring-brand-400/20;
  }
  html[data-theme="light"] .input,
  html[data-theme="light"] .select,
  html[data-theme="light"] textarea.input-area {
    @apply border-slate-200 bg-white text-slate-800;
  }
  .prose-chat h2 {
    @apply text-base font-semibold text-brand-300 mb-1 mt-2;
  }
  .prose-chat h3 {
    @apply text-sm font-semibold text-slate-200 mb-1 mt-2;
  }
  .prose-chat p {
    @apply mb-2 leading-relaxed;
  }
  .prose-chat ul {
    @apply list-disc pl-5 mb-2 space-y-0.5;
  }
  .prose-chat code {
    @apply rounded bg-black/30 px-1.5 py-0.5 font-mono text-xs;
  }
  .prose-chat strong {
    @apply font-semibold text-white;
  }
  html[data-theme="light"] .prose-chat strong {
    @apply text-slate-900;
  }
}

@layer utilities {
  .no-scrollbar::-webkit-scrollbar {
    display: none;
  }
  .no-scrollbar {
    -ms-overflow-style: none;
    scrollbar-width: none;
  }
}
